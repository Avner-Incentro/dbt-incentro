version: 2

sources:
  - name: jaffle_shop
    description: a clone of a Postgres application database
    database: analytics
    schema: dbt_mmalendes
    tables:
      - name: customers
        description: raw customers data
        columns:
          - name: id
            description: primary key
            
      - name: orders
        description: raw orders data
        columns:
          - name: id
            description: primary key